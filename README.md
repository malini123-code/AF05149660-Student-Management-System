# AF05149660-Student-Management-System
The Student Management System is a Java JDBC-based application used to manage student data efficiently. It performs CRUD operations like adding, viewing, updating, and deleting records. It connects to a MySQL database and uses a structured approach with packages, making it simple, organized, and useful for learning database programming.
